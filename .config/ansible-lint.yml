---
skip_list:
  - fqcn-builtins # Shorter names look much cleaner
  - no-handler # Handlers are useful, but split up the code flow too much when
               # their extra features aren't needed
  - var-naming[no-role-prefix] # The Proxmox role requires specific names
