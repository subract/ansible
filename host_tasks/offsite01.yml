---
- name: Install zfs prereqs
  tags: pkg, zfs
  package:
    name:
      - raspberrypi-kernel-headers

- name: Install zfs
  tags: pkg, zfs
  package:
    name:
      - zfs-dkms
      - zfsutils-linux